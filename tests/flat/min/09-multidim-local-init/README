# 09-multidim-local-init —— 多维局部数组的**初始化 + 逐维下标**

## 这对用例锁住什么

直接钉住三处**真** bug（改之前 gcc 与**两条赛道**的结果都不一样）：

| 形状 | gcc | 改前（结构化 / 平面） |
|---|---|---|
| `int c[2][3]={{1,2,3},{4,5,6}}; return c[1][2];` | **6** | 3 / 3 |
| `int c[2][1][3]={{},{2,1,8}}; return c[1][0][1]+c[1][0][2]*10;` | **81** | 0 / 0 |
| `int c[2][2][2]={{{1,2},{3,4}},{{5,6},{7,8}}}; return c[1][1][0]*10+c[0][1][1];` | **74** | 0 / 0 |

三处根因（都在报告 §十）：
1. `genIndexChain` 在 GEP 之前**又乘了一次**元素个数（旧线性模型的遗留）；
2. 初始化地址把**字节偏移 ÷ 元素宽度**当成**扁平下标**（多维下标不是线性的）；
3. `emitMemcpyConst` 的元素宽度操作数**写死成 1**（常量池是 `[N x i32]`）。

外加类型错误：`alloca` 拿到了**值**的类型（数组 → 指向数组的指针）
⇒ `int[2][1][3]` 被分配成 `ptr[[2 x ptr[[1 x ptr[[3 x i32]]]]]]`。

## ★ 为什么这对用例必须存在

这三处 bug **两条赛道一致地错** —— "平面与结构化行为等价"这条判据对它们
**完全无感**。判据只能是**拿 gcc 的真值对照**（`bad.sy` 就是那个错形状）。

## good / bad 只差一处

`bad.sy` 把第二次下标写成 `c[0][0][2]`（其余逐字节相同）⇒ 期望值不同、
但两份**都是合法 IR** ⇒ 这条对照同时钉住"下标算得对"。
