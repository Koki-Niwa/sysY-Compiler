// 多维局部数组的初始化 + 逐维下标。
//   与 good.sy **只差一处**：最后一个下标读的是 c[1][0][2] 两次
//   ⇒ 8 + 8*10 = 88（gcc）—— 用来钉住"第二个下标真的生效"。
int main() {
  int c[2][1][3] = {{}, {2, 1, 8}};
  return c[1][0][2] + c[1][0][2] * 10;
}
