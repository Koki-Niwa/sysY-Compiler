// 多维局部数组的初始化 + 逐维下标。
//   c[1][0][1] = 1, c[1][0][2] = 8  ⇒  1 + 8*10 = 81（gcc 的实跑值）
//   ⚠️ 改之前**两条赛道都算 0** —— 所以判据只能是"等于 gcc"，不是"两边相等"。
int main() {
  int c[2][1][3] = {{}, {2, 1, 8}};
  return c[1][0][1] + c[1][0][2] * 10;
}
