# 10-array-param-index —— **数组形参**的下标（槽里装的是指针）

## 锁住什么

    int g[3] = {7, 8, 9};
    int first(int a[]) { return a[0]*100 + a[1]*10 + a[2]; }
    int main() { int b[3] = {1, 2, 3}; return first(g) + first(b); }

| | gcc | 改前 结构化/平面 |
|---|---|---|
| `first(g)+first(b)` = 789+123 | **912** | 0 / 0 |
| 只 `first(b)` = 123 | **123** | 0 / 0 |

根因：`int a[]` 的参数在 IR 里是 `alloca ptr[i32]`（S05：形参也一律在内存里），
所以 `a[i]` 必须**先把那根指针 load 出来**再索引。第一版直接在**槽的地址**上
按 `i32` 索引 ⇒ 读到的是**指针本身**，不是它指向的数组。

★ 这条特别重要：`putstr` 这类**每个程序都用**的函数就是这么写的 ——
它一错，输出直接不对（实测 `80_chaos_token.sy`：结构化 1002 个字符、
平面 460 个，且内容不同）。而且**两条赛道一致地错**，"行为等价"看不出来。

## good / bad 只差一处

`bad.sy` 只改最后一行的常量（`+ first(b)` → `+ 0`），其余逐字节相同。
