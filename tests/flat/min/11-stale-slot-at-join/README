# 11-stale-slot-at-join —— 汇合点的 φ 拿了**陈旧的槽值**

## 锁住什么

```c
int ans = 7;
if (ans < 0) ans = -ans;
putint(ans);
```

| | gcc | 结构化 | 平面（改前/改后） |
|---|---|---|---|
| 输出 | **7** | 7 | **0** / 7 |

`t1.sy` / `t0.sy` 的缩小版：真实用例是 `tests/prelim_*/performance/transpose0..2.sy`
（结构化 100、平面 0）与 `tests/final_arm/performance/gaussian-2.sy`（`out` 相同、
**全局内存摘要不同**）—— 都在"结果类差异"里。

## 根因（展平器）

`if` 的汇合点由 `enterJoin` 建 φ，入值取**各分支的环境快照**。而环境
`env_[slot]` **只在 `Store` 时更新**：条件里发出的那条 `%6 = load ans`
**不进环境**。于是 else 分支（空）的环境里 `ans` 仍指向**初始化时的常量**
（`i32 3`，值是 0）—— 它在 SSA 语义上**支配一切**，所以 `V3` 的
"φ 入值必须支配对应前驱块"**查不出来**；轨 A（往返）与轨 C（结构合法）
对"值语义错"也全盲。

产出（改前）：

```
L1:                     ; then
  %8 = load i32, ptr[i32] %1
  %9 = sub i32 %3, %8
  store i32 %9, ptr[i32] %1
  br L3
L2:                     ; else（空）
  br L3
L3:
  %0 = phi i32 [(%9 L1), (%3 L2)]   ; ← %3 是前导里的常量 0，应为 %6
```

## 修法方向（两次失败的尝试都记在这里）

判据只能是"**这条分支上这个槽的内容**"，而它由**内存**决定：

1. ❌ 在 `Load` 时把读出来的值写进 `env_` —— 会让"只读来源"的槽变成
   **循环携带候选** ⇒ `26_scope4.sy` 的外层 `while(1)` 凭空多一个 φ、回边指向
   并不跳回它的块 ⇒ `V3` 报"入值 2 vs 前驱 1"。全量：321→295（结果类 14→28）。
2. ❌ 记"读/写序号"再在汇合点取舍 —— `enterBlock` 不接管序号簿记，
   `frameExitOrds_` 快照拿到的是**别的分支**的序号（实测 else 侧写序 = then 侧的 4）。
3. ⏳ 正确方向：汇合时对"该分支没写过"的槽**在前驱块补发一条 `load`**
   （内存才是权威），判据用"入边环境值 == **分支分歧点**的值"。注意
   `lowerIf` 的 `pre` 必须在**条件降低之后**取（条件里的 `load` 要算进去）。

## good / bad 只差一处

`bad.sy` 把初始化常量 `7` 换成 `0` —— 于是"陈旧值"与"正确值"**恰好相同**，
输出不再是 0。这正是"两形状一致"抓不住这类缺陷的原因：**缺陷只在
'旧值 ≠ 新值' 的输入上显形**。
